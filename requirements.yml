---
collections:
  # Essential collections for most automation
  - name: community.general
    version: ">=7.0.0"
  
  - name: ansible.posix
    version: ">=1.5.0"
  
  - name: community.crypto
    version: ">=2.0.0"

# Add roles here if needed
# roles:
#   - name: geerlingguy.docker
#     version: ">=6.0.0"
